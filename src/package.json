{
    "private": true,
    "type": "module",
    "scripts": {
        "add-env": "cp -n .env.example .env || true",
        "dev": "vite",
        "build": "vite build",
        "postinstall": "npm run add-env"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.5",
        "vite": "^4.0.0"
    }
}
